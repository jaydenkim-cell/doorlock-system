# Supabase 실시간 동기화를 켜려면 아래 값을 채워 .env 로 복사하세요.
# 값이 없으면 앱은 자동으로 "이 기기에만 저장(localStorage)" 모드로 동작합니다.
# (Supabase 대시보드 > Project Settings > API 에서 확인)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# 가족 공용 여행방 코드(한 가족이 같은 값을 쓰면 같은 데이터를 공유).
# 비워두면 기본값 'choi70' 사용.
VITE_TRIP_ID=choi70
